---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="About us - LowKeyDevs"
  description="Learn about the philosophy behind LowKeyDevs: why we build fast, privacy-first, 100% client-side developer utility tools."
>
  <Header />

  <main id="main-content" class="about-main container">
    <header class="about-header max-w-narrow mx-auto w-full">
      <h1 class="about-title" data-i18n="about.title" data-i18n-html="true">Why we built <span class="accent-text">LowKeyDevs</span></h1>
      <p class="about-subtitle" data-i18n="about.subtitle">The story and core principles behind the fastest toolbox on the web.</p>
    </header>

    <section class="about-content">
      <div class="about-grid">
        <article class="about-card">
          <h2 data-i18n="about.missionTitle">Our mission</h2>
          <p data-i18n="about.missionText1">
            Developer utility tools are everywhere. But almost all of them suffer from the same issues: intrusive ads, cookie popups, slow loading speeds, and forced account registrations.
          </p>
          <p data-i18n="about.missionText2">
            We built <strong>LowKeyDevs</strong> to solve these pain points. We want it to be the first place you visit whenever you need to format JSON, generate a password, or decode base64. A clean, fast utility that simply works.
          </p>
        </article>

        <article class="about-card">
          <h2 data-i18n="about.principlesTitle">The core principles</h2>
          <ul class="principles-list">
            <li>
              <strong data-i18n="about.principleInstantTitle">Instant</strong>: <span data-i18n="about.principleInstantText">No unnecessary loading. Paste your content, and results render immediately.</span>
            </li>
            <li>
              <strong data-i18n="about.principleClientTitle">100% client-side</strong>: <span data-i18n="about.principleClientText">Your privacy is absolute. All computations run in your own browser; no sensitive data touches a server.</span>
            </li>
            <li>
              <strong data-i18n="about.principleCleanTitle">Clean UX</strong>: <span data-i18n="about.principleCleanText">No dark patterns, fake countdowns, popups, or aggressive tracking pixels. Trust is our biggest feature.</span>
            </li>
          </ul>
        </article>
      </div>

      <div class="about-cta max-w-narrow mx-auto w-full">
        <h2 data-i18n="about.ctaTitle">Open source & community driven</h2>
        <p data-i18n="about.ctaText">
          LowKeyDevs is designed to scale to hundreds of utilities. We are continuously adding new tools. Have an idea or need a specific converter? Let us know!
        </p>
        <a href="/" class="btn btn-primary" data-i18n="about.ctaButton">Browse all tools</a>
      </div>
    </section>
  </main>

  <Footer />
</Layout>

<style>
  .about-main {
    padding-top: clamp(32px, 8vw, 64px);
    padding-bottom: clamp(40px, 10vw, 80px);
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: clamp(24px, 6vw, 48px);
  }

  .about-header {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    text-align: center;
  }

  .about-title {
    font-family: var(--font-heading);
    font-size: clamp(28px, 5vw, 54px);
    font-weight: 700;
    letter-spacing: -0.03em;
    color: var(--text-primary);
  }

  .accent-text {
    color: var(--accent);
  }

  .about-subtitle {
    color: var(--text-secondary);
    font-size: clamp(15px, 1.5vw, 18px);
    font-weight: 400;
    line-height: 1.7;
  }

  .about-content {
    display: flex;
    flex-direction: column;
    gap: clamp(24px, 6vw, 48px);
  }

  .about-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: clamp(16px, 4vw, 24px);
  }

  @media (max-width: 768px) {
    .about-grid {
      grid-template-columns: 1fr;
    }
  }

  .about-card {
    padding: clamp(16px, 5vw, 32px);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .about-card h2 {
    font-family: var(--font-heading);
    font-size: 20px;
    font-weight: 600;
    color: var(--text-primary);
  }

  .about-card p {
    color: var(--text-secondary);
    font-size: 15px;
    line-height: 1.7;
  }

  .principles-list {
    list-style-type: none;
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
  }

  .principles-list li {
    font-size: 15px;
    color: var(--text-secondary);
    line-height: 1.7;
    position: relative;
    padding-left: var(--space-6);
  }

  .principles-list li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--success);
    font-weight: bold;
    font-size: 16px;
  }

  .about-cta {
    padding: clamp(24px, 6vw, 40px) clamp(16px, 5vw, 32px);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-5);
    background-color: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
  }

  .about-cta h2 {
    font-family: var(--font-heading);
    font-size: 20px;
    font-weight: 600;
    color: var(--text-primary);
  }

  .about-cta p {
    color: var(--text-secondary);
    font-size: 15px;
    max-width: 460px;
    line-height: 1.7;
  }

  @media (max-width: 480px) {
    .about-cta {
      padding: var(--space-8) var(--space-4);
    }
  }
</style>
